'use strict'

import React, { Component, PropTypes } from 'react'
import { connect } from 'react-redux'


class SolutionSelector extends Component {
  render () {
    return (
      <div> hi</div>
    )
  }
}

const mapStateToProps = (state) => {}

export default connect(mapStateToProps)(SolutionSelector)

